# Chapter 06 — Real-time systems

**Goals:** **Interruption** (cooperative cancel), **turn-taking** mental model, cheap **VAD** vs neural VAD, **duplex** sketch, and **echo_mode** session snapshot.

For production-grade **Silero VAD**, install optional deps (`torch`) per root `pyproject.toml` optional group `vad` — see [PLAN.md](../PLAN.md).

**Next:** [07_tools/](../07_tools/).
