# Chapter 04 — Agent core

**Goals:** Run a **local GGUF** LLM with a **Qwen-style chat prompt**, optional **memory** strings, and **debug** the prompt without touching audio.

Requires `models/llm/qwen2.5-0.5b-instruct-q4_k_m.gguf` from `00_start_here/download_models.py`.

**Next:** [05_full_voice_loop/](../05_full_voice_loop/).
